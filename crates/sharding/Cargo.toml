[package]
name = "sharding"
version = "0.1.0"
edition = "2021"

[features]
testing = ["dep:rand"]

[dependencies]
indexmap = "2"
thiserror = "1"
rand = { version = "0.8", optional = true }

[dev-dependencies]
rand = "0.8"

[lints]
workspace = true

